import { ScrollprogressDirective } from './scrollprogress.directive';

describe('ScrollprogressDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollprogressDirective();
    expect(directive).toBeTruthy();
  });
});
